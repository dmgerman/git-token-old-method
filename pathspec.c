begin_unit
begin_include
include|#
directive|include
file|"cache.h"
end_include
begin_include
include|#
directive|include
file|"dir.h"
end_include
begin_include
include|#
directive|include
file|"pathspec.h"
end_include
begin_comment
comment|/*  * Finds which of the given pathspecs match items in the index.  *  * For each pathspec, sets the corresponding entry in the seen[] array  * (which should be specs items long, i.e. the same size as pathspec)  * to the nature of the "closest" (i.e. most specific) match found for  * that pathspec in the index, if it was a closer type of match than  * the existing entry.  As an optimization, matching is skipped  * altogether if seen[] already only contains non-zero entries.  *  * If seen[] has not already been written to, it may make sense  * to use find_pathspecs_matching_against_index() instead.  */
end_comment
begin_function
DECL|function|add_pathspec_matches_against_index
name|void
name|add_pathspec_matches_against_index
parameter_list|(
specifier|const
name|char
modifier|*
modifier|*
name|pathspec
parameter_list|,
name|char
modifier|*
name|seen
parameter_list|,
name|int
name|specs
parameter_list|)
block|{
name|int
name|num_unmatched
init|=
literal|0
decl_stmt|,
name|i
decl_stmt|;
comment|/* 	 * Since we are walking the index as if we were walking the directory, 	 * we have to mark the matched pathspec as seen; otherwise we will 	 * mistakenly think that the user gave a pathspec that did not match 	 * anything. 	 */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|specs
condition|;
name|i
operator|++
control|)
if|if
condition|(
operator|!
name|seen
index|[
name|i
index|]
condition|)
name|num_unmatched
operator|++
expr_stmt|;
if|if
condition|(
operator|!
name|num_unmatched
condition|)
return|return;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|active_nr
condition|;
name|i
operator|++
control|)
block|{
name|struct
name|cache_entry
modifier|*
name|ce
init|=
name|active_cache
index|[
name|i
index|]
decl_stmt|;
name|match_pathspec
argument_list|(
name|pathspec
argument_list|,
name|ce
operator|->
name|name
argument_list|,
name|ce_namelen
argument_list|(
name|ce
argument_list|)
argument_list|,
literal|0
argument_list|,
name|seen
argument_list|)
expr_stmt|;
block|}
block|}
end_function
begin_comment
comment|/*  * Finds which of the given pathspecs match items in the index.  *  * This is a one-shot wrapper around add_pathspec_matches_against_index()  * which allocates, populates, and returns a seen[] array indicating the  * nature of the "closest" (i.e. most specific) matches which each of the  * given pathspecs achieves against all items in the index.  */
end_comment
begin_function
DECL|function|find_pathspecs_matching_against_index
name|char
modifier|*
name|find_pathspecs_matching_against_index
parameter_list|(
specifier|const
name|char
modifier|*
modifier|*
name|pathspec
parameter_list|)
block|{
name|char
modifier|*
name|seen
decl_stmt|;
name|int
name|i
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|pathspec
index|[
name|i
index|]
condition|;
name|i
operator|++
control|)
empty_stmt|;
comment|/* just counting */
name|seen
operator|=
name|xcalloc
argument_list|(
name|i
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|add_pathspec_matches_against_index
argument_list|(
name|pathspec
argument_list|,
name|seen
argument_list|,
name|i
argument_list|)
expr_stmt|;
return|return
name|seen
return|;
block|}
end_function
begin_comment
comment|/*  * Check the index to see whether path refers to a submodule, or  * something inside a submodule.  If the former, returns the path with  * any trailing slash stripped.  If the latter, dies with an error  * message.  */
end_comment
begin_function
DECL|function|check_path_for_gitlink
specifier|const
name|char
modifier|*
name|check_path_for_gitlink
parameter_list|(
specifier|const
name|char
modifier|*
name|path
parameter_list|)
block|{
name|int
name|i
decl_stmt|,
name|path_len
init|=
name|strlen
argument_list|(
name|path
argument_list|)
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|active_nr
condition|;
name|i
operator|++
control|)
block|{
name|struct
name|cache_entry
modifier|*
name|ce
init|=
name|active_cache
index|[
name|i
index|]
decl_stmt|;
if|if
condition|(
name|S_ISGITLINK
argument_list|(
name|ce
operator|->
name|ce_mode
argument_list|)
condition|)
block|{
name|int
name|ce_len
init|=
name|ce_namelen
argument_list|(
name|ce
argument_list|)
decl_stmt|;
if|if
condition|(
name|path_len
operator|<=
name|ce_len
operator|||
name|path
index|[
name|ce_len
index|]
operator|!=
literal|'/'
operator|||
name|memcmp
argument_list|(
name|ce
operator|->
name|name
argument_list|,
name|path
argument_list|,
name|ce_len
argument_list|)
condition|)
comment|/* path does not refer to this 				 * submodule or anything inside it */
continue|continue;
if|if
condition|(
name|path_len
operator|==
name|ce_len
operator|+
literal|1
condition|)
block|{
comment|/* path refers to submodule; 				 * strip trailing slash */
return|return
name|xstrndup
argument_list|(
name|ce
operator|->
name|name
argument_list|,
name|ce_len
argument_list|)
return|;
block|}
else|else
block|{
name|die
argument_list|(
name|_
argument_list|(
literal|"Path '%s' is in submodule '%.*s'"
argument_list|)
argument_list|,
name|path
argument_list|,
name|ce_len
argument_list|,
name|ce
operator|->
name|name
argument_list|)
expr_stmt|;
block|}
block|}
block|}
return|return
name|path
return|;
block|}
end_function
begin_comment
comment|/*  * Dies if the given path refers to a file inside a symlinked  * directory in the index.  */
end_comment
begin_function
DECL|function|die_if_path_beyond_symlink
name|void
name|die_if_path_beyond_symlink
parameter_list|(
specifier|const
name|char
modifier|*
name|path
parameter_list|,
specifier|const
name|char
modifier|*
name|prefix
parameter_list|)
block|{
if|if
condition|(
name|has_symlink_leading_path
argument_list|(
name|path
argument_list|,
name|strlen
argument_list|(
name|path
argument_list|)
argument_list|)
condition|)
block|{
name|int
name|len
init|=
name|prefix
condition|?
name|strlen
argument_list|(
name|prefix
argument_list|)
else|:
literal|0
decl_stmt|;
name|die
argument_list|(
name|_
argument_list|(
literal|"'%s' is beyond a symbolic link"
argument_list|)
argument_list|,
name|path
operator|+
name|len
argument_list|)
expr_stmt|;
block|}
block|}
end_function
begin_comment
comment|/*  * Magic pathspec  *  * NEEDSWORK: These need to be moved to dir.h or even to a new  * pathspec.h when we restructure get_pathspec() users to use the  * "struct pathspec" interface.  *  * Possible future magic semantics include stuff like:  *  *	{ PATHSPEC_NOGLOB, '!', "noglob" },  *	{ PATHSPEC_ICASE, '\0', "icase" },  *	{ PATHSPEC_RECURSIVE, '*', "recursive" },  *	{ PATHSPEC_REGEXP, '\0', "regexp" },  *  */
end_comment
begin_define
DECL|macro|PATHSPEC_FROMTOP
define|#
directive|define
name|PATHSPEC_FROMTOP
value|(1<<0)
end_define
begin_struct
DECL|struct|pathspec_magic
specifier|static
struct|struct
name|pathspec_magic
block|{
DECL|member|bit
name|unsigned
name|bit
decl_stmt|;
DECL|member|mnemonic
name|char
name|mnemonic
decl_stmt|;
comment|/* this cannot be ':'! */
DECL|member|name
specifier|const
name|char
modifier|*
name|name
decl_stmt|;
block|}
DECL|variable|pathspec_magic
name|pathspec_magic
index|[]
init|=
block|{
block|{
name|PATHSPEC_FROMTOP
block|,
literal|'/'
block|,
literal|"top"
block|}
block|, }
struct|;
end_struct
begin_comment
comment|/*  * Take an element of a pathspec and check for magic signatures.  * Append the result to the prefix.  *  * For now, we only parse the syntax and throw out anything other than  * "top" magic.  *  * NEEDSWORK: This needs to be rewritten when we start migrating  * get_pathspec() users to use the "struct pathspec" interface.  For  * example, a pathspec element may be marked as case-insensitive, but  * the prefix part must always match literally, and a single stupid  * string cannot express such a case.  */
end_comment
begin_function
DECL|function|prefix_pathspec
specifier|static
specifier|const
name|char
modifier|*
name|prefix_pathspec
parameter_list|(
specifier|const
name|char
modifier|*
name|prefix
parameter_list|,
name|int
name|prefixlen
parameter_list|,
specifier|const
name|char
modifier|*
name|elt
parameter_list|)
block|{
name|unsigned
name|magic
init|=
literal|0
decl_stmt|;
specifier|const
name|char
modifier|*
name|copyfrom
init|=
name|elt
decl_stmt|;
name|int
name|i
decl_stmt|;
if|if
condition|(
name|elt
index|[
literal|0
index|]
operator|!=
literal|':'
condition|)
block|{
empty_stmt|;
comment|/* nothing to do */
block|}
elseif|else
if|if
condition|(
name|elt
index|[
literal|1
index|]
operator|==
literal|'('
condition|)
block|{
comment|/* longhand */
specifier|const
name|char
modifier|*
name|nextat
decl_stmt|;
for|for
control|(
name|copyfrom
operator|=
name|elt
operator|+
literal|2
init|;
operator|*
name|copyfrom
operator|&&
operator|*
name|copyfrom
operator|!=
literal|')'
condition|;
name|copyfrom
operator|=
name|nextat
control|)
block|{
name|size_t
name|len
init|=
name|strcspn
argument_list|(
name|copyfrom
argument_list|,
literal|",)"
argument_list|)
decl_stmt|;
if|if
condition|(
name|copyfrom
index|[
name|len
index|]
operator|==
literal|','
condition|)
name|nextat
operator|=
name|copyfrom
operator|+
name|len
operator|+
literal|1
expr_stmt|;
else|else
comment|/* handle ')' and '\0' */
name|nextat
operator|=
name|copyfrom
operator|+
name|len
expr_stmt|;
if|if
condition|(
operator|!
name|len
condition|)
continue|continue;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|ARRAY_SIZE
argument_list|(
name|pathspec_magic
argument_list|)
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|strlen
argument_list|(
name|pathspec_magic
index|[
name|i
index|]
operator|.
name|name
argument_list|)
operator|==
name|len
operator|&&
operator|!
name|strncmp
argument_list|(
name|pathspec_magic
index|[
name|i
index|]
operator|.
name|name
argument_list|,
name|copyfrom
argument_list|,
name|len
argument_list|)
condition|)
block|{
name|magic
operator||=
name|pathspec_magic
index|[
name|i
index|]
operator|.
name|bit
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|ARRAY_SIZE
argument_list|(
name|pathspec_magic
argument_list|)
operator|<=
name|i
condition|)
name|die
argument_list|(
literal|"Invalid pathspec magic '%.*s' in '%s'"
argument_list|,
operator|(
name|int
operator|)
name|len
argument_list|,
name|copyfrom
argument_list|,
name|elt
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
operator|*
name|copyfrom
operator|!=
literal|')'
condition|)
name|die
argument_list|(
literal|"Missing ')' at the end of pathspec magic in '%s'"
argument_list|,
name|elt
argument_list|)
expr_stmt|;
name|copyfrom
operator|++
expr_stmt|;
block|}
else|else
block|{
comment|/* shorthand */
for|for
control|(
name|copyfrom
operator|=
name|elt
operator|+
literal|1
init|;
operator|*
name|copyfrom
operator|&&
operator|*
name|copyfrom
operator|!=
literal|':'
condition|;
name|copyfrom
operator|++
control|)
block|{
name|char
name|ch
init|=
operator|*
name|copyfrom
decl_stmt|;
if|if
condition|(
operator|!
name|is_pathspec_magic
argument_list|(
name|ch
argument_list|)
condition|)
break|break;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|ARRAY_SIZE
argument_list|(
name|pathspec_magic
argument_list|)
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|pathspec_magic
index|[
name|i
index|]
operator|.
name|mnemonic
operator|==
name|ch
condition|)
block|{
name|magic
operator||=
name|pathspec_magic
index|[
name|i
index|]
operator|.
name|bit
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|ARRAY_SIZE
argument_list|(
name|pathspec_magic
argument_list|)
operator|<=
name|i
condition|)
name|die
argument_list|(
literal|"Unimplemented pathspec magic '%c' in '%s'"
argument_list|,
name|ch
argument_list|,
name|elt
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
operator|*
name|copyfrom
operator|==
literal|':'
condition|)
name|copyfrom
operator|++
expr_stmt|;
block|}
if|if
condition|(
name|magic
operator|&
name|PATHSPEC_FROMTOP
condition|)
return|return
name|xstrdup
argument_list|(
name|copyfrom
argument_list|)
return|;
else|else
return|return
name|prefix_path
argument_list|(
name|prefix
argument_list|,
name|prefixlen
argument_list|,
name|copyfrom
argument_list|)
return|;
block|}
end_function
begin_comment
comment|/*  * N.B. get_pathspec() is deprecated in favor of the "struct pathspec"  * based interface - see pathspec_magic above.  *  * Arguments:  *  - prefix - a path relative to the root of the working tree  *  - pathspec - a list of paths underneath the prefix path  *  * Iterates over pathspec, prepending each path with prefix,  * and return the resulting list.  *  * If pathspec is empty, return a singleton list containing prefix.  *  * If pathspec and prefix are both empty, return an empty list.  *  * This is typically used by built-in commands such as add.c, in order  * to normalize argv arguments provided to the built-in into a list of  * paths to process, all relative to the root of the working tree.  */
end_comment
begin_function
DECL|function|get_pathspec
specifier|const
name|char
modifier|*
modifier|*
name|get_pathspec
parameter_list|(
specifier|const
name|char
modifier|*
name|prefix
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|pathspec
parameter_list|)
block|{
specifier|const
name|char
modifier|*
name|entry
init|=
operator|*
name|pathspec
decl_stmt|;
specifier|const
name|char
modifier|*
modifier|*
name|src
decl_stmt|,
modifier|*
modifier|*
name|dst
decl_stmt|;
name|int
name|prefixlen
decl_stmt|;
if|if
condition|(
operator|!
name|prefix
operator|&&
operator|!
name|entry
condition|)
return|return
name|NULL
return|;
if|if
condition|(
operator|!
name|entry
condition|)
block|{
specifier|static
specifier|const
name|char
modifier|*
name|spec
index|[
literal|2
index|]
decl_stmt|;
name|spec
index|[
literal|0
index|]
operator|=
name|prefix
expr_stmt|;
name|spec
index|[
literal|1
index|]
operator|=
name|NULL
expr_stmt|;
return|return
name|spec
return|;
block|}
comment|/* Otherwise we have to re-write the entries.. */
name|src
operator|=
name|pathspec
expr_stmt|;
name|dst
operator|=
name|pathspec
expr_stmt|;
name|prefixlen
operator|=
name|prefix
condition|?
name|strlen
argument_list|(
name|prefix
argument_list|)
else|:
literal|0
expr_stmt|;
while|while
condition|(
operator|*
name|src
condition|)
block|{
operator|*
operator|(
name|dst
operator|++
operator|)
operator|=
name|prefix_pathspec
argument_list|(
name|prefix
argument_list|,
name|prefixlen
argument_list|,
operator|*
name|src
argument_list|)
expr_stmt|;
name|src
operator|++
expr_stmt|;
block|}
operator|*
name|dst
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
operator|!
operator|*
name|pathspec
condition|)
return|return
name|NULL
return|;
return|return
name|pathspec
return|;
block|}
end_function
end_unit
