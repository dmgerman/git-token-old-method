begin_unit
begin_comment
comment|/*  * GIT - The information manager from hell  *  * Copyright (C) Linus Torvalds, 2005  */
end_comment
begin_include
include|#
directive|include
file|"cache.h"
end_include
begin_function
DECL|function|safe_create_dir
specifier|static
name|void
name|safe_create_dir
parameter_list|(
specifier|const
name|char
modifier|*
name|dir
parameter_list|)
block|{
if|if
condition|(
name|mkdir
argument_list|(
name|dir
argument_list|,
literal|0755
argument_list|)
operator|<
literal|0
condition|)
block|{
if|if
condition|(
name|errno
operator|!=
name|EEXIST
condition|)
block|{
name|perror
argument_list|(
name|dir
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
block|}
end_function
begin_comment
comment|/*  * If you want to, you can share the DB area with any number of branches.  * That has advantages: you can save space by sharing all the SHA1 objects.  * On the other hand, it might just make lookup slower and messier. You  * be the judge.  The default case is to have one DB per managed directory.  */
end_comment
begin_function
DECL|function|main
name|int
name|main
parameter_list|(
name|int
name|argc
parameter_list|,
name|char
modifier|*
modifier|*
name|argv
parameter_list|)
block|{
specifier|const
name|char
modifier|*
name|sha1_dir
decl_stmt|;
name|char
modifier|*
name|path
decl_stmt|;
name|int
name|len
decl_stmt|,
name|i
decl_stmt|;
name|sha1_dir
operator|=
name|get_object_directory
argument_list|()
expr_stmt|;
if|if
condition|(
operator|!
name|gitenv
argument_list|(
name|DB_ENVIRONMENT
argument_list|)
operator|&&
operator|!
name|gitenv
argument_list|(
name|GIT_DIR_ENVIRONMENT
argument_list|)
condition|)
block|{
comment|/* We create leading paths only when we fall back 		 * to local .git/objects, at least for now. 		 */
name|safe_create_dir
argument_list|(
name|DEFAULT_GIT_DIR_ENVIRONMENT
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"defaulting to local storage area\n"
argument_list|)
expr_stmt|;
block|}
name|len
operator|=
name|strlen
argument_list|(
name|sha1_dir
argument_list|)
expr_stmt|;
name|path
operator|=
name|xmalloc
argument_list|(
name|len
operator|+
literal|40
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|path
argument_list|,
name|sha1_dir
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|safe_create_dir
argument_list|(
name|sha1_dir
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|256
condition|;
name|i
operator|++
control|)
block|{
name|sprintf
argument_list|(
name|path
operator|+
name|len
argument_list|,
literal|"/%02x"
argument_list|,
name|i
argument_list|)
expr_stmt|;
name|safe_create_dir
argument_list|(
name|path
argument_list|)
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
end_function
end_unit
