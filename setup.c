begin_unit
begin_include
include|#
directive|include
file|"cache.h"
end_include
begin_function
DECL|function|get_pathspec
specifier|const
name|char
modifier|*
modifier|*
name|get_pathspec
parameter_list|(
specifier|const
name|char
modifier|*
name|prefix
parameter_list|,
name|char
modifier|*
modifier|*
name|pathspec
parameter_list|)
block|{
name|char
modifier|*
name|entry
init|=
operator|*
name|pathspec
decl_stmt|;
name|char
modifier|*
modifier|*
name|p
decl_stmt|;
name|int
name|prefixlen
decl_stmt|;
if|if
condition|(
operator|!
name|prefix
condition|)
block|{
name|char
modifier|*
modifier|*
name|p
decl_stmt|;
if|if
condition|(
operator|!
name|entry
condition|)
return|return
name|NULL
return|;
name|p
operator|=
name|pathspec
expr_stmt|;
do|do
block|{
if|if
condition|(
operator|*
name|entry
operator|!=
literal|'.'
condition|)
continue|continue;
comment|/* fixup ? */
block|}
do|while
condition|(
operator|(
name|entry
operator|=
operator|*
operator|++
name|p
operator|)
operator|!=
name|NULL
condition|)
do|;
return|return
operator|(
specifier|const
name|char
operator|*
operator|*
operator|)
name|pathspec
return|;
block|}
if|if
condition|(
operator|!
name|entry
condition|)
block|{
specifier|static
specifier|const
name|char
modifier|*
name|spec
index|[
literal|2
index|]
decl_stmt|;
name|spec
index|[
literal|0
index|]
operator|=
name|prefix
expr_stmt|;
name|spec
index|[
literal|1
index|]
operator|=
name|NULL
expr_stmt|;
return|return
name|spec
return|;
block|}
comment|/* Otherwise we have to re-write the entries.. */
name|prefixlen
operator|=
name|strlen
argument_list|(
name|prefix
argument_list|)
expr_stmt|;
name|p
operator|=
name|pathspec
expr_stmt|;
do|do
block|{
name|int
name|speclen
decl_stmt|,
name|len
init|=
name|prefixlen
decl_stmt|;
name|char
modifier|*
name|n
decl_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
operator|!
name|strcmp
argument_list|(
name|entry
argument_list|,
literal|"."
argument_list|)
condition|)
block|{
name|entry
operator|++
expr_stmt|;
break|break;
block|}
if|if
condition|(
operator|!
name|strncmp
argument_list|(
name|entry
argument_list|,
literal|"./"
argument_list|,
literal|2
argument_list|)
condition|)
block|{
name|entry
operator|+=
literal|2
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
operator|!
name|strncmp
argument_list|(
name|entry
argument_list|,
literal|"../"
argument_list|,
literal|3
argument_list|)
condition|)
block|{
do|do
block|{
if|if
condition|(
operator|!
name|len
condition|)
name|die
argument_list|(
literal|"'%s' is outside repository"
argument_list|,
operator|*
name|p
argument_list|)
expr_stmt|;
name|len
operator|--
expr_stmt|;
block|}
do|while
condition|(
name|len
operator|&&
name|prefix
index|[
name|len
operator|-
literal|1
index|]
operator|!=
literal|'/'
condition|)
do|;
name|entry
operator|+=
literal|3
expr_stmt|;
continue|continue;
block|}
break|break;
block|}
name|speclen
operator|=
name|strlen
argument_list|(
name|entry
argument_list|)
expr_stmt|;
name|n
operator|=
name|xmalloc
argument_list|(
name|speclen
operator|+
name|len
operator|+
literal|1
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|n
argument_list|,
name|prefix
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|n
operator|+
name|len
argument_list|,
name|entry
argument_list|,
name|speclen
operator|+
literal|1
argument_list|)
expr_stmt|;
operator|*
name|p
operator|=
name|n
expr_stmt|;
block|}
do|while
condition|(
operator|(
name|entry
operator|=
operator|*
operator|++
name|p
operator|)
operator|!=
name|NULL
condition|)
do|;
return|return
operator|(
specifier|const
name|char
operator|*
operator|*
operator|)
name|pathspec
return|;
block|}
end_function
begin_function
DECL|function|setup_git_directory
specifier|const
name|char
modifier|*
name|setup_git_directory
parameter_list|(
name|void
parameter_list|)
block|{
specifier|static
name|char
name|cwd
index|[
name|PATH_MAX
operator|+
literal|1
index|]
decl_stmt|;
name|int
name|len
decl_stmt|,
name|offset
decl_stmt|;
comment|/* 	 * If GIT_DIR is set explicitly, we're not going 	 * to do any discovery 	 */
if|if
condition|(
name|gitenv
argument_list|(
name|GIT_DIR_ENVIRONMENT
argument_list|)
condition|)
return|return
name|NULL
return|;
if|if
condition|(
operator|!
name|getcwd
argument_list|(
name|cwd
argument_list|,
sizeof|sizeof
argument_list|(
name|cwd
argument_list|)
argument_list|)
operator|||
name|cwd
index|[
literal|0
index|]
operator|!=
literal|'/'
condition|)
name|die
argument_list|(
literal|"Unable to read current working directory"
argument_list|)
expr_stmt|;
name|offset
operator|=
name|len
operator|=
name|strlen
argument_list|(
name|cwd
argument_list|)
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
comment|/* 		 * We always want to see a .git/refs/ subdirectory 		 */
if|if
condition|(
operator|!
name|access
argument_list|(
literal|".git/refs/"
argument_list|,
name|X_OK
argument_list|)
condition|)
block|{
comment|/* 			 * Then we need either a GIT_OBJECT_DIRECTORY define 			 * or a .git/objects/ directory 			 */
if|if
condition|(
name|gitenv
argument_list|(
name|DB_ENVIRONMENT
argument_list|)
operator|||
operator|!
name|access
argument_list|(
literal|".git/objects/"
argument_list|,
name|X_OK
argument_list|)
condition|)
break|break;
block|}
name|chdir
argument_list|(
literal|".."
argument_list|)
expr_stmt|;
do|do
block|{
if|if
condition|(
operator|!
name|offset
condition|)
name|die
argument_list|(
literal|"Not a git repository"
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
name|cwd
index|[
operator|--
name|offset
index|]
operator|!=
literal|'/'
condition|)
do|;
block|}
if|if
condition|(
name|offset
operator|==
name|len
condition|)
return|return
name|NULL
return|;
comment|/* Make "offset" point to past the '/', and add a '/' at the end */
name|offset
operator|++
expr_stmt|;
name|cwd
index|[
name|len
operator|++
index|]
operator|=
literal|'/'
expr_stmt|;
name|cwd
index|[
name|len
index|]
operator|=
literal|0
expr_stmt|;
return|return
name|cwd
operator|+
name|offset
return|;
block|}
end_function
end_unit
