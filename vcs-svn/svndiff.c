begin_unit
begin_comment
comment|/*  * Licensed under a two-clause BSD-style license.  * See LICENSE for details.  */
end_comment
begin_include
include|#
directive|include
file|"git-compat-util.h"
end_include
begin_include
include|#
directive|include
file|"line_buffer.h"
end_include
begin_include
include|#
directive|include
file|"svndiff.h"
end_include
begin_comment
comment|/*  * svndiff0 applier  *  * See http://svn.apache.org/repos/asf/subversion/trunk/notes/svndiff.  *  * svndiff0 ::= 'SVN\0' window*  */
end_comment
begin_function
DECL|function|error_short_read
specifier|static
name|int
name|error_short_read
parameter_list|(
name|struct
name|line_buffer
modifier|*
name|input
parameter_list|)
block|{
if|if
condition|(
name|buffer_ferror
argument_list|(
name|input
argument_list|)
condition|)
return|return
name|error
argument_list|(
literal|"error reading delta: %s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
return|;
return|return
name|error
argument_list|(
literal|"invalid delta: unexpected end of file"
argument_list|)
return|;
block|}
end_function
begin_function
DECL|function|read_magic
specifier|static
name|int
name|read_magic
parameter_list|(
name|struct
name|line_buffer
modifier|*
name|in
parameter_list|,
name|off_t
modifier|*
name|len
parameter_list|)
block|{
specifier|static
specifier|const
name|char
name|magic
index|[]
init|=
block|{
literal|'S'
block|,
literal|'V'
block|,
literal|'N'
block|,
literal|'\0'
block|}
decl_stmt|;
name|struct
name|strbuf
name|sb
init|=
name|STRBUF_INIT
decl_stmt|;
if|if
condition|(
operator|*
name|len
operator|<
sizeof|sizeof
argument_list|(
name|magic
argument_list|)
operator|||
name|buffer_read_binary
argument_list|(
name|in
argument_list|,
operator|&
name|sb
argument_list|,
sizeof|sizeof
argument_list|(
name|magic
argument_list|)
argument_list|)
operator|!=
sizeof|sizeof
argument_list|(
name|magic
argument_list|)
condition|)
return|return
name|error_short_read
argument_list|(
name|in
argument_list|)
return|;
if|if
condition|(
name|memcmp
argument_list|(
name|sb
operator|.
name|buf
argument_list|,
name|magic
argument_list|,
sizeof|sizeof
argument_list|(
name|magic
argument_list|)
argument_list|)
condition|)
return|return
name|error
argument_list|(
literal|"invalid delta: unrecognized file type"
argument_list|)
return|;
operator|*
name|len
operator|-=
sizeof|sizeof
argument_list|(
name|magic
argument_list|)
expr_stmt|;
name|strbuf_release
argument_list|(
operator|&
name|sb
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function
begin_function
DECL|function|svndiff0_apply
name|int
name|svndiff0_apply
parameter_list|(
name|struct
name|line_buffer
modifier|*
name|delta
parameter_list|,
name|off_t
name|delta_len
parameter_list|,
name|struct
name|sliding_view
modifier|*
name|preimage
parameter_list|,
name|FILE
modifier|*
name|postimage
parameter_list|)
block|{
name|assert
argument_list|(
name|delta
operator|&&
name|preimage
operator|&&
name|postimage
argument_list|)
expr_stmt|;
if|if
condition|(
name|read_magic
argument_list|(
name|delta
argument_list|,
operator|&
name|delta_len
argument_list|)
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
name|delta_len
condition|)
return|return
name|error
argument_list|(
literal|"What do you think I am?  A delta applier?"
argument_list|)
return|;
return|return
literal|0
return|;
block|}
end_function
end_unit
